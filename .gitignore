# Claude Code local settings
.claude/settings.local.json

# PlatformIO
.pio/

# Agent worktrees (temporary; merged to main before removal)
.worktrees/

# Config contains sensitive data (MQTT credentials) — use data/config.example.json as template
data/config.json

# VSCode — exclude generated/personal files but keep shared tasks
.vscode/*
!.vscode/tasks.json
